version: '3'

services:
  webserver:
    build: ./requirements/nginx
    image: apigeon/nginx
    ports:
      - 8080:443
    restart: always
    volumes:
      - ./requirements/nginx/conf/:/etc/nginx/conf.d/:ro
      - ./requirements/nginx/cert/:/etc/ssl/:ro
